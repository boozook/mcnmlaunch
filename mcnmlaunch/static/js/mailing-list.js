$(document).ready(function() {	function subscribeUs () {		// update user interface		$('#response').html('Подождите, идёт добавление вашего e-mail адреса...');		// Prepare query string and send AJAX request		$.ajax({            type: "POST",            url: "/store",			data: {				email: $('#email').attr('value')			},			success: function(data) {				$('#response').slideDown('fast').html(data.msg);				if (data.success) {					$('#email').val('')                    _gaq.push(["_trackPageview", "/#my-ajax-page-store"]);				}			}		});	}	$('a.submit').click(function() {		subscribeUs();		return false;	});	$('#email').keypress(function (event) {		// Для отключения стандарного невменяемого попапа от mailchimp		if (event.which == '13') {			event.preventDefault();			subscribeUs();		}	});});